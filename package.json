{
  "name": "@bitbonsai/pi-memory",
  "version": "0.1.0",
  "description": "Small persistent memory extension for Pi.",
  "keywords": [
    "pi-package",
    "extension",
    "memory",
    "learning",
    "preferences"
  ],
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bitbonsai/pi-memory"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "test": "node --test --import tsx src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --sourcemap --packages=external --external:@mariozechner/pi-coding-agent --external:@earendil-works/pi-coding-agent --external:@sinclair/typebox",
    "dev": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --sourcemap --watch --packages=external --external:@mariozechner/pi-coding-agent --external:@earendil-works/pi-coding-agent --external:@sinclair/typebox",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@sinclair/typebox": "*"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@sinclair/typebox": "^0.34.48",
    "@types/node": "^22.15.21",
    "esbuild": "^0.27.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
